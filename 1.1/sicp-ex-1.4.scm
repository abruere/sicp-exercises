;; SICP exercise 1.4

;; b is added (if statement return + operator) to a if the former has a positive value
;; or it is substracted otherwise (if statement return - operator),
;; which boils down to adding the absolute value of b to a: (+ a |b|).